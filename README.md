# 🧩 Учебная виртуальная машина (УВМ), вариант 23  
## Ассемблер и интерпретатор

Данный проект реализует **ассемблер** и **интерпретатор** для учебной виртуальной машины (УВМ), вариант 23.  
Ассемблер преобразует текстовую программу в бинарный машинный код, а интерпретатор выполняет этот код, эмулируя работу виртуальной машины и формируя дамп памяти.

---

## 📁 Структура проекта

```
.
├── uvm23_text.py      # Ассемблер (текст → бинарный код)
├── uvm23_inter.py     # Интерпретатор (выполнение бинарного кода)
├── uvm23-test.txt     # Пример входной программы
├── uvm23-output.bin   # Скомпилированный бинарный файл
├── dump.csv           # Дамп памяти
└── README.md          # Документация проекта
```

---

## ⚙️ Спецификация УВМ (вариант 23)

Все команды имеют **фиксированный размер 4 байта**  
Кодирование выполняется в формате **little-endian**.

---

### 1️⃣ Загрузка константы — `const`

| Поле | Биты | Значение |
|-----|------|----------|
| A   | 0–6  | `12` |
| B   | 7–21 | Константа |

- **Операнд:** поле `B`  
- **Результат:** регистр-аккумулятор  

**Тест:**  
```
const(12, 208)
```
Бинарное представление:
```
0x0C, 0x68, 0x00, 0x00
```

---

### 2️⃣ Чтение значения из памяти — `read`

| Поле | Биты | Значение |
|-----|------|----------|
| A   | 0–6  | `106` |
| B   | 7–31 | Адрес памяти |

- **Операнд:** значение по адресу `B`  
- **Результат:** регистр-аккумулятор  

**Тест:**  
```
read(106, 225)
```
Бинарное представление:
```
0xEA, 0x70, 0x00, 0x00
```

---

### 3️⃣ Запись значения в память — `write`

| Поле | Биты | Значение |
|-----|------|----------|
| A   | 0–6  | `95` |
| B   | 7–31 | Адрес памяти |

- **Операнд:** регистр-аккумулятор  
- **Результат:** запись в память  

**Тест:**  
```
write(95, 421)
```
Бинарное представление:
```
0xDF, 0xD2, 0x00, 0x00
```

---

### 4️⃣ Унарная операция `abs`

| Поле | Биты | Значение |
|-----|------|----------|
| A   | 0–6  | `85` |
| B   | 7–14 | Смещение |

- **Адрес:** `регистр + B`  
- **Результат:** `abs(значение из памяти)` → регистр  

**Тест:**  
```
abs(85, 90)
```
Бинарное представление:
```
0x55, 0x2D, 0x00, 0x00
```

---

## 🧪 Формат входной программы

Входной файл — обычный текстовый файл.  
Одна команда — одна строка.

Пример:

```text
# Пример программы для УВМ
const(12, 208)
read(106, 225)
write(95, 421)
abs(85, 90)
```

---

## 🛠 Ассемблер — `uvm23_text.py`

### Запуск
```bash
python uvm23_text.py -i uvm23-test.txt -o uvm23-output.bin -t 1
```

---

## ▶️ Интерпретатор — `uvm23_inter.py`

### Запуск
```bash
python uvm23_inter.py -i uvm23-output.bin -d dump.csv -r 0-50
```

---

## 📄 Формат дампа памяти

```csv
Address,Value
0,0
1,0
2,15
```

---

## 🎓 Назначение проекта

Учебный проект для изучения принципов работы виртуальных машин, ассемблеров и интерпретаторов.

---

**Вариант:** 23  
**Язык:** Python  
